1. lp-editorial-dark.html
Catégorie : Editorial Magazine — Usage : Portfolio, presse, contenu long, dark mode

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};          /* #0a0a0a */
      --color-surface: {{COLOR_SURFACE}}; /* #111111 */
      --color-accent: {{COLOR_ACCENT}};   /* #ffffff */
      --color-muted: {{COLOR_MUTED}};     /* #555555 */
      --color-text: {{COLOR_TEXT}};       /* #eeeeee */
      --color-label: {{COLOR_LABEL}};     /* #888888 */
      --font-main: {{FONT_MAIN}};         /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 20px 40px;
      border-bottom: 1px solid #1e1e1e;
      position: sticky;
      top: 0;
      background: var(--color-bg);
      z-index: 100;
    }
    .header__logo img { height: 28px; width: auto; }
    .header__nav { display: flex; gap: 32px; }
    .header__nav a {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: var(--color-label);
      transition: color .2s;
    }
    .header__nav a:hover { color: var(--color-accent); }
    .header__cta {
      background: var(--color-accent);
      color: var(--color-bg);
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      padding: 10px 20px;
    }

    /* ─── HERO ───────────────────────────────────── */
    .hero {
      padding: 80px 40px 60px;
      max-width: 640px;
    }
    .hero__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-label);
      margin-bottom: 20px;
    }
    .hero__title {
      font-size: 42px;
      font-weight: 700;
      line-height: 1.1;
      letter-spacing: -1px;
      margin-bottom: 20px;
    }
    .hero__body {
      font-size: 15px;
      color: #aaaaaa;
      margin-bottom: 32px;
      max-width: 480px;
    }
    .btn {
      display: inline-block;
      padding: 12px 28px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--primary { background: var(--color-accent); color: var(--color-bg); }
    .btn--outline { border: 1px solid #333; color: var(--color-text); }

    /* ─── IMAGE FULL ─────────────────────────────── */
    .img-full { width: 100%; height: 380px; overflow: hidden; }

    /* ─── SECTION FEATURE ────────────────────────── */
    .section-feature {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0;
      border-top: 1px solid #1e1e1e;
    }
    .section-feature__image { height: 320px; overflow: hidden; }
    .section-feature__content {
      padding: 48px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .section-feature__content.reverse { order: -1; }
    .section-feature__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-label);
      margin-bottom: 16px;
    }
    .section-feature__title {
      font-size: 26px;
      font-weight: 700;
      line-height: 1.2;
      margin-bottom: 16px;
    }
    .section-feature__body {
      font-size: 13px;
      color: #888888;
      margin-bottom: 24px;
      line-height: 1.8;
    }

    /* ─── GRID 3 COL ─────────────────────────────── */
    .section-grid {
      padding: 60px 40px;
      border-top: 1px solid #1e1e1e;
    }
    .section-grid__header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 32px;
    }
    .section-grid__title { font-size: 18px; font-weight: 700; }
    .section-grid__link { font-size: 12px; color: var(--color-label); text-decoration: underline; }
    .cards { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
    .card__image { height: 200px; overflow: hidden; margin-bottom: 16px; }
    .card__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--color-label);
      margin-bottom: 8px;
    }
    .card__title { font-size: 15px; font-weight: 700; line-height: 1.3; margin-bottom: 8px; }
    .card__excerpt { font-size: 12px; color: #777777; line-height: 1.7; }

    /* ─── NEWSLETTER ─────────────────────────────── */
    .section-newsletter {
      background: var(--color-surface);
      padding: 64px 40px;
      border-top: 1px solid #1e1e1e;
      text-align: center;
    }
    .section-newsletter__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-label);
      margin-bottom: 16px;
    }
    .section-newsletter__title { font-size: 28px; font-weight: 700; margin-bottom: 12px; }
    .section-newsletter__body { font-size: 13px; color: #888888; margin-bottom: 32px; }
    .form-inline {
      display: flex;
      gap: 0;
      max-width: 440px;
      margin: 0 auto;
    }
    .form-inline input {
      flex: 1;
      padding: 14px 18px;
      font-size: 13px;
      background: #1a1a1a;
      border: 1px solid #333;
      border-right: none;
      color: var(--color-text);
      outline: none;
    }
    .form-inline input::placeholder { color: #555; }
    .form-inline button {
      padding: 14px 24px;
      background: var(--color-accent);
      color: var(--color-bg);
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1px;
      border: none;
      cursor: pointer;
    }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 32px 40px;
      border-top: 1px solid #1e1e1e;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer__logo img { height: 22px; width: auto; }
    .footer__links { display: flex; gap: 24px; }
    .footer__links a { font-size: 11px; color: #555; }
    .footer__links a:hover { color: #fff; }
    .footer__legal { font-size: 11px; color: #444; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <nav class="header__nav">
      <a href="{{NAV_LINK_1_URL}}">{{NAV_LINK_1_LABEL}}</a>
      <a href="{{NAV_LINK_2_URL}}">{{NAV_LINK_2_LABEL}}</a>
      <a href="{{NAV_LINK_3_URL}}">{{NAV_LINK_3_LABEL}}</a>
    </nav>
    <a href="{{HEADER_CTA_URL}}" class="btn btn--primary">{{HEADER_CTA_LABEL}}</a>
  </header>

  <!-- HERO -->
  <section class="hero">
    <p class="hero__label">{{HERO_CATEGORY_LABEL}}</p>
    <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
    <p class="hero__body">{{HERO_BODY}}</p>
    <a href="{{HERO_CTA_URL}}" class="btn btn--primary">{{HERO_CTA_LABEL}}</a>
  </section>

  <!-- HERO IMAGE -->
  <div class="img-full">
    <img src="{{HERO_IMAGE_URL}}" alt="{{HERO_IMAGE_ALT}}" />
  </div>

  <!-- FEATURE SECTION 1 — image left -->
  <section class="section-feature">
    <div class="section-feature__image">
      <img src="{{FEATURE_1_IMAGE_URL}}" alt="{{FEATURE_1_TITLE}}" />
    </div>
    <div class="section-feature__content">
      <p class="section-feature__label">{{FEATURE_1_LABEL}}</p>
      <h2 class="section-feature__title">{{FEATURE_1_TITLE}}</h2>
      <p class="section-feature__body">{{FEATURE_1_BODY}}</p>
      <a href="{{FEATURE_1_CTA_URL}}" class="btn btn--outline">{{FEATURE_1_CTA_LABEL}}</a>
    </div>
  </section>

  <!-- FEATURE SECTION 2 — image right -->
  <section class="section-feature">
    <div class="section-feature__content reverse">
      <p class="section-feature__label">{{FEATURE_2_LABEL}}</p>
      <h2 class="section-feature__title">{{FEATURE_2_TITLE}}</h2>
      <p class="section-feature__body">{{FEATURE_2_BODY}}</p>
      <a href="{{FEATURE_2_CTA_URL}}" class="btn btn--outline">{{FEATURE_2_CTA_LABEL}}</a>
    </div>
    <div class="section-feature__image">
      <img src="{{FEATURE_2_IMAGE_URL}}" alt="{{FEATURE_2_TITLE}}" />
    </div>
  </section>

  <!-- CARDS GRID -->
  <section class="section-grid">
    <div class="section-grid__header">
      <h2 class="section-grid__title">{{GRID_SECTION_TITLE}}</h2>
      <a href="{{GRID_VIEW_ALL_URL}}" class="section-grid__link">{{GRID_VIEW_ALL_LABEL}}</a>
    </div>
    <div class="cards">
      <div class="card">
        <div class="card__image"><img src="{{CARD_1_IMAGE_URL}}" alt="{{CARD_1_TITLE}}" /></div>
        <p class="card__label">{{CARD_1_CATEGORY}}</p>
        <h3 class="card__title">{{CARD_1_TITLE}}</h3>
        <p class="card__excerpt">{{CARD_1_EXCERPT}}</p>
      </div>
      <div class="card">
        <div class="card__image"><img src="{{CARD_2_IMAGE_URL}}" alt="{{CARD_2_TITLE}}" /></div>
        <p class="card__label">{{CARD_2_CATEGORY}}</p>
        <h3 class="card__title">{{CARD_2_TITLE}}</h3>
        <p class="card__excerpt">{{CARD_2_EXCERPT}}</p>
      </div>
      <div class="card">
        <div class="card__image"><img src="{{CARD_3_IMAGE_URL}}" alt="{{CARD_3_TITLE}}" /></div>
        <p class="card__label">{{CARD_3_CATEGORY}}</p>
        <h3 class="card__title">{{CARD_3_TITLE}}</h3>
        <p class="card__excerpt">{{CARD_3_EXCERPT}}</p>
      </div>
    </div>
  </section>

  <!-- NEWSLETTER -->
  <section class="section-newsletter">
    <p class="section-newsletter__label">{{NEWSLETTER_LABEL}}</p>
    <h2 class="section-newsletter__title">{{NEWSLETTER_TITLE}}</h2>
    <p class="section-newsletter__body">{{NEWSLETTER_BODY}}</p>
    <form class="form-inline" action="{{FORM_ACTION_URL}}" method="POST">
      <input type="email" name="email" placeholder="{{FORM_PLACEHOLDER}}" required />
      <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
    </form>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__links">
      <a href="{{FOOTER_LINK_1_URL}}">{{FOOTER_LINK_1_LABEL}}</a>
      <a href="{{FOOTER_LINK_2_URL}}">{{FOOTER_LINK_2_LABEL}}</a>
      <a href="{{FOOTER_LINK_3_URL}}">{{FOOTER_LINK_3_LABEL}}</a>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

2. lp-content-newsletter.html
Catégorie : Content Newsletter — Usage : Blog, capture d'emails, articles curatés

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};          /* #f0fce8 */
      --color-surface: {{COLOR_SURFACE}}; /* #ffffff */
      --color-accent: {{COLOR_ACCENT}};   /* #3dba00 */
      --color-dark: {{COLOR_DARK}};       /* #111111 */
      --color-muted: {{COLOR_MUTED}};     /* #666666 */
      --font-main: {{FONT_MAIN}};         /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-dark);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 40px;
      background: var(--color-surface);
      border-bottom: 1px solid #e0eedd;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .header__logo img { height: 26px; width: auto; }
    .header__cta {
      background: var(--color-accent);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      padding: 10px 22px;
      border-radius: 2px;
    }

    /* ─── HERO ───────────────────────────────────── */
    .hero {
      padding: 64px 40px 40px;
      max-width: 700px;
    }
    .hero__tag {
      display: inline-block;
      background: var(--color-accent);
      color: #fff;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      padding: 4px 12px;
      margin-bottom: 20px;
    }
    .hero__title {
      font-size: 40px;
      font-weight: 700;
      line-height: 1.15;
      letter-spacing: -0.5px;
      margin-bottom: 16px;
    }
    .hero__body {
      font-size: 15px;
      color: var(--color-muted);
      margin-bottom: 28px;
      max-width: 520px;
    }
    .btn {
      display: inline-block;
      padding: 12px 28px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--primary { background: var(--color-accent); color: #fff; border-radius: 2px; }
    .btn--dark { background: var(--color-dark); color: #fff; border-radius: 2px; }

    /* ─── ARTICLES GRID ──────────────────────────── */
    .section-articles {
      padding: 48px 40px;
      background: var(--color-surface);
    }
    .section-articles__header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 28px;
    }
    .section-title {
      font-size: 13px;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--color-muted);
    }
    .section-link { font-size: 12px; color: var(--color-accent); text-decoration: underline; }
    .articles-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; }
    .article-card__image { height: 200px; overflow: hidden; margin-bottom: 14px; border-radius: 4px; }
    .article-card__tag {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--color-accent);
      margin-bottom: 8px;
    }
    .article-card__title {
      font-size: 17px;
      font-weight: 700;
      line-height: 1.3;
      margin-bottom: 8px;
    }
    .article-card__excerpt { font-size: 13px; color: var(--color-muted); }

    /* ─── NEWSLETTER CTA ─────────────────────────── */
    .section-newsletter {
      padding: 56px 40px;
      background: var(--color-bg);
    }
    .newsletter-box {
      background: var(--color-surface);
      border: 1px solid #d4eece;
      border-radius: 4px;
      padding: 48px 40px;
      text-align: center;
      max-width: 560px;
      margin: 0 auto;
    }
    .newsletter-box__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-muted);
      margin-bottom: 12px;
    }
    .newsletter-box__title { font-size: 26px; font-weight: 700; margin-bottom: 12px; }
    .newsletter-box__body { font-size: 13px; color: var(--color-muted); margin-bottom: 28px; }
    .form-inline { display: flex; gap: 0; }
    .form-inline input {
      flex: 1;
      padding: 13px 16px;
      font-size: 13px;
      border: 1px solid #cce8c4;
      border-right: none;
      outline: none;
      background: #f8fef5;
    }
    .form-inline input::placeholder { color: #aaa; }
    .form-inline button {
      padding: 13px 22px;
      background: var(--color-accent);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      border: none;
      cursor: pointer;
    }

    /* ─── LIST SECTION ───────────────────────────── */
    .section-list { padding: 56px 40px; background: var(--color-surface); }
    .list-items { display: flex; flex-direction: column; gap: 20px; }
    .list-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-bottom: 20px;
      border-bottom: 1px solid #eee;
    }
    .list-item:last-child { border-bottom: none; }
    .list-item__num {
      font-size: 32px;
      font-weight: 900;
      color: #e5f5e0;
      min-width: 48px;
    }
    .list-item__content { flex: 1; padding: 0 24px; }
    .list-item__title { font-size: 16px; font-weight: 700; margin-bottom: 6px; }
    .list-item__desc { font-size: 13px; color: var(--color-muted); }
    .list-item__link { font-size: 12px; color: var(--color-accent); text-decoration: underline; white-space: nowrap; }

    /* ─── BANNER CTA ─────────────────────────────── */
    .section-banner {
      position: relative;
      height: 260px;
      overflow: hidden;
    }
    .section-banner img { width: 100%; height: 100%; object-fit: cover; }
    .section-banner__overlay {
      position: absolute;
      inset: 0;
      background: rgba(0,0,0,.55);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 32px;
      color: #fff;
    }
    .section-banner__title { font-size: 28px; font-weight: 700; margin-bottom: 20px; }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 28px 40px;
      background: var(--color-dark);
      color: #666;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer__logo img { height: 22px; width: auto; filter: invert(1); }
    .footer__dots { display: flex; gap: 6px; }
    .footer__dot {
      width: 7px; height: 7px; border-radius: 50%;
      background: #444;
    }
    .footer__dot.active { background: #fff; }
    .footer__legal { font-size: 11px; color: #444; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <a href="{{HEADER_CTA_URL}}" class="btn btn--primary">{{HEADER_CTA_LABEL}}</a>
  </header>

  <!-- HERO -->
  <section class="hero">
    <span class="hero__tag">{{HERO_TAG_LABEL}}</span>
    <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
    <p class="hero__body">{{HERO_BODY}}</p>
    <a href="{{HERO_CTA_URL}}" class="btn btn--primary">{{HERO_CTA_LABEL}}</a>
  </section>

  <!-- ARTICLES GRID -->
  <section class="section-articles">
    <div class="section-articles__header">
      <p class="section-title">{{ARTICLES_SECTION_LABEL}}</p>
      <a href="{{ARTICLES_VIEW_ALL_URL}}" class="section-link">{{ARTICLES_VIEW_ALL_LABEL}}</a>
    </div>
    <div class="articles-grid">
      <div class="article-card">
        <div class="article-card__image"><img src="{{ARTICLE_1_IMAGE_URL}}" alt="{{ARTICLE_1_TITLE}}" /></div>
        <p class="article-card__tag">{{ARTICLE_1_CATEGORY}}</p>
        <h3 class="article-card__title">{{ARTICLE_1_TITLE}}</h3>
        <p class="article-card__excerpt">{{ARTICLE_1_EXCERPT}}</p>
      </div>
      <div class="article-card">
        <div class="article-card__image"><img src="{{ARTICLE_2_IMAGE_URL}}" alt="{{ARTICLE_2_TITLE}}" /></div>
        <p class="article-card__tag">{{ARTICLE_2_CATEGORY}}</p>
        <h3 class="article-card__title">{{ARTICLE_2_TITLE}}</h3>
        <p class="article-card__excerpt">{{ARTICLE_2_EXCERPT}}</p>
      </div>
    </div>
  </section>

  <!-- NEWSLETTER -->
  <section class="section-newsletter">
    <div class="newsletter-box">
      <p class="newsletter-box__label">{{NEWSLETTER_LABEL}}</p>
      <h2 class="newsletter-box__title">{{NEWSLETTER_TITLE}}</h2>
      <p class="newsletter-box__body">{{NEWSLETTER_BODY}}</p>
      <form class="form-inline" action="{{FORM_ACTION_URL}}" method="POST">
        <input type="email" name="email" placeholder="{{FORM_PLACEHOLDER}}" required />
        <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
      </form>
    </div>
  </section>

  <!-- LIST SECTION -->
  <section class="section-list">
    <p class="section-title" style="margin-bottom:28px;">{{LIST_SECTION_LABEL}}</p>
    <div class="list-items">
      <div class="list-item">
        <span class="list-item__num">01</span>
        <div class="list-item__content">
          <h3 class="list-item__title">{{LIST_1_TITLE}}</h3>
          <p class="list-item__desc">{{LIST_1_DESC}}</p>
        </div>
        <a href="{{LIST_1_URL}}" class="list-item__link">{{LIST_1_CTA}}</a>
      </div>
      <div class="list-item">
        <span class="list-item__num">02</span>
        <div class="list-item__content">
          <h3 class="list-item__title">{{LIST_2_TITLE}}</h3>
          <p class="list-item__desc">{{LIST_2_DESC}}</p>
        </div>
        <a href="{{LIST_2_URL}}" class="list-item__link">{{LIST_2_CTA}}</a>
      </div>
      <div class="list-item">
        <span class="list-item__num">03</span>
        <div class="list-item__content">
          <h3 class="list-item__title">{{LIST_3_TITLE}}</h3>
          <p class="list-item__desc">{{LIST_3_DESC}}</p>
        </div>
        <a href="{{LIST_3_URL}}" class="list-item__link">{{LIST_3_CTA}}</a>
      </div>
    </div>
  </section>

  <!-- BANNER CTA -->
  <section class="section-banner">
    <img src="{{BANNER_IMAGE_URL}}" alt="{{BANNER_ALT}}" />
    <div class="section-banner__overlay">
      <h2 class="section-banner__title">{{BANNER_HEADLINE}}</h2>
      <a href="{{BANNER_CTA_URL}}" class="btn btn--primary">{{BANNER_CTA_LABEL}}</a>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__dots">
      <div class="footer__dot active"></div>
      <div class="footer__dot"></div>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

3. lp-creative-portfolio.html
Catégorie : Creative Portfolio — Usage : Agence créative, photographe, studio, capture newsletter

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};          /* #fff8f0 */
      --color-dark: {{COLOR_DARK}};       /* #111111 */
      --color-accent: {{COLOR_ACCENT}};   /* #e8500a */
      --color-muted: {{COLOR_MUTED}};     /* #888888 */
      --font-main: {{FONT_MAIN}};         /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-dark);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 40px;
      background: var(--color-bg);
      border-bottom: 1px solid #e8e0d8;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .header__logo img { height: 26px; width: auto; }
    .header__nav { display: flex; gap: 28px; }
    .header__nav a {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: var(--color-muted);
    }
    .header__nav a:hover { color: var(--color-dark); }

    /* ─── HERO ───────────────────────────────────── */
    .hero { padding: 60px 40px 40px; }
    .hero__title {
      font-size: 44px;
      font-weight: 700;
      line-height: 1.1;
      letter-spacing: -1px;
      max-width: 600px;
      margin-bottom: 20px;
    }
    .hero__body {
      font-size: 15px;
      color: var(--color-muted);
      max-width: 480px;
      margin-bottom: 32px;
    }
    .btn {
      display: inline-block;
      padding: 12px 28px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--accent { background: var(--color-accent); color: #fff; }
    .btn--dark { background: var(--color-dark); color: #fff; }

    /* ─── MOSAIC GRID ────────────────────────────── */
    .section-mosaic { padding: 48px 40px; }
    .mosaic {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr;
      grid-template-rows: 240px 240px;
      gap: 12px;
    }
    .mosaic__item { overflow: hidden; }
    .mosaic__item--tall { grid-row: span 2; }
    .mosaic__item img { transition: transform .4s; }
    .mosaic__item:hover img { transform: scale(1.04); }

    /* ─── NEWSLETTER DARK ────────────────────────── */
    .section-newsletter {
      background: var(--color-dark);
      padding: 56px 40px;
      text-align: center;
      color: #fff;
    }
    .section-newsletter__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: #888;
      margin-bottom: 16px;
    }
    .section-newsletter__title { font-size: 28px; font-weight: 700; margin-bottom: 12px; }
    .section-newsletter__body { font-size: 13px; color: #888; margin-bottom: 28px; }
    .form-inline { display: flex; gap: 0; max-width: 440px; margin: 0 auto; }
    .form-inline input {
      flex: 1;
      padding: 13px 18px;
      font-size: 13px;
      background: #1a1a1a;
      border: 1px solid #333;
      border-right: none;
      color: #fff;
      outline: none;
    }
    .form-inline input::placeholder { color: #555; }
    .form-inline button {
      padding: 13px 24px;
      background: var(--color-accent);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      border: none;
      cursor: pointer;
    }

    /* ─── GRID 2+2 ───────────────────────────────── */
    .section-work { padding: 56px 40px; background: var(--color-bg); }
    .section-work__header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 28px;
    }
    .section-label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-muted);
    }
    .work-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; }
    .work-card__image { height: 220px; overflow: hidden; margin-bottom: 14px; }
    .work-card__title { font-size: 16px; font-weight: 700; margin-bottom: 6px; }
    .work-card__desc { font-size: 12px; color: var(--color-muted); }

    /* ─── QUOTE ──────────────────────────────────── */
    .section-quote {
      padding: 64px 80px;
      text-align: center;
      background: #fff;
      border-top: 1px solid #efe7dc;
      border-bottom: 1px solid #efe7dc;
    }
    .section-quote__text {
      font-size: 24px;
      font-weight: 300;
      line-height: 1.5;
      color: var(--color-dark);
      margin-bottom: 16px;
    }
    .section-quote__author { font-size: 12px; font-weight: 700; color: var(--color-muted); }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 28px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top: 1px solid #e8e0d8;
    }
    .footer__logo img { height: 22px; width: auto; }
    .footer__dots { display: flex; gap: 6px; }
    .footer__dot { width: 7px; height: 7px; border-radius: 50%; background: #ccc; }
    .footer__dot.active { background: var(--color-dark); }
    .footer__legal { font-size: 11px; color: #aaa; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <nav class="header__nav">
      <a href="{{NAV_LINK_1_URL}}">{{NAV_LINK_1_LABEL}}</a>
      <a href="{{NAV_LINK_2_URL}}">{{NAV_LINK_2_LABEL}}</a>
      <a href="{{NAV_LINK_3_URL}}">{{NAV_LINK_3_LABEL}}</a>
    </nav>
  </header>

  <!-- HERO -->
  <section class="hero">
    <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
    <p class="hero__body">{{HERO_BODY}}</p>
    <a href="{{HERO_CTA_URL}}" class="btn btn--accent">{{HERO_CTA_LABEL}}</a>
  </section>

  <!-- MOSAIC -->
  <section class="section-mosaic">
    <div class="mosaic">
      <div class="mosaic__item mosaic__item--tall">
        <img src="{{MOSAIC_IMG_1_URL}}" alt="{{MOSAIC_IMG_1_ALT}}" />
      </div>
      <div class="mosaic__item">
        <img src="{{MOSAIC_IMG_2_URL}}" alt="{{MOSAIC_IMG_2_ALT}}" />
      </div>
      <div class="mosaic__item">
        <img src="{{MOSAIC_IMG_3_URL}}" alt="{{MOSAIC_IMG_3_ALT}}" />
      </div>
      <div class="mosaic__item">
        <img src="{{MOSAIC_IMG_4_URL}}" alt="{{MOSAIC_IMG_4_ALT}}" />
      </div>
      <div class="mosaic__item">
        <img src="{{MOSAIC_IMG_5_URL}}" alt="{{MOSAIC_IMG_5_ALT}}" />
      </div>
    </div>
  </section>

  <!-- NEWSLETTER -->
  <section class="section-newsletter">
    <p class="section-newsletter__label">{{NEWSLETTER_LABEL}}</p>
    <h2 class="section-newsletter__title">{{NEWSLETTER_TITLE}}</h2>
    <p class="section-newsletter__body">{{NEWSLETTER_BODY}}</p>
    <form class="form-inline" action="{{FORM_ACTION_URL}}" method="POST">
      <input type="email" name="email" placeholder="{{FORM_PLACEHOLDER}}" required />
      <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
    </form>
  </section>

  <!-- WORK GRID -->
  <section class="section-work">
    <div class="section-work__header">
      <p class="section-label">{{WORK_SECTION_LABEL}}</p>
      <a href="{{WORK_VIEW_ALL_URL}}" style="font-size:12px; color:var(--color-accent); text-decoration:underline;">{{WORK_VIEW_ALL_LABEL}}</a>
    </div>
    <div class="work-grid">
      <div class="work-card">
        <div class="work-card__image"><img src="{{WORK_1_IMAGE_URL}}" alt="{{WORK_1_TITLE}}" /></div>
        <h3 class="work-card__title">{{WORK_1_TITLE}}</h3>
        <p class="work-card__desc">{{WORK_1_DESC}}</p>
      </div>
      <div class="work-card">
        <div class="work-card__image"><img src="{{WORK_2_IMAGE_URL}}" alt="{{WORK_2_TITLE}}" /></div>
        <h3 class="work-card__title">{{WORK_2_TITLE}}</h3>
        <p class="work-card__desc">{{WORK_2_DESC}}</p>
      </div>
      <div class="work-card">
        <div class="work-card__image"><img src="{{WORK_3_IMAGE_URL}}" alt="{{WORK_3_TITLE}}" /></div>
        <h3 class="work-card__title">{{WORK_3_TITLE}}</h3>
        <p class="work-card__desc">{{WORK_3_DESC}}</p>
      </div>
      <div class="work-card">
        <div class="work-card__image"><img src="{{WORK_4_IMAGE_URL}}" alt="{{WORK_4_TITLE}}" /></div>
        <h3 class="work-card__title">{{WORK_4_TITLE}}</h3>
        <p class="work-card__desc">{{WORK_4_DESC}}</p>
      </div>
    </div>
  </section>

  <!-- QUOTE -->
  <section class="section-quote">
    <p class="section-quote__text">"{{QUOTE_TEXT}}"</p>
    <p class="section-quote__author">— {{QUOTE_AUTHOR}}</p>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__dots">
      <div class="footer__dot active"></div>
      <div class="footer__dot"></div>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

4. lp-event-registration.html
Catégorie : Event Registration — Usage : Conférence, webinar, meetup, speakers

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};            /* #f0ecff */
      --color-primary: {{COLOR_PRIMARY}};   /* #7c3aed */
      --color-dark: {{COLOR_DARK}};         /* #111111 */
      --color-surface: {{COLOR_SURFACE}};   /* #ffffff */
      --color-muted: {{COLOR_MUTED}};       /* #888888 */
      --font-main: {{FONT_MAIN}};           /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-dark);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 40px;
      background: var(--color-primary);
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .header__logo img { height: 26px; width: auto; filter: brightness(0) invert(1); }
    .header__cta {
      background: #fff;
      color: var(--color-primary);
      font-size: 12px;
      font-weight: 700;
      padding: 10px 22px;
    }

    /* ─── HERO ───────────────────────────────────── */
    .hero {
      background: var(--color-primary);
      padding: 64px 40px 56px;
      color: #fff;
    }
    .hero__tag {
      display: inline-block;
      border: 1px solid rgba(255,255,255,.4);
      color: rgba(255,255,255,.8);
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      padding: 4px 12px;
      margin-bottom: 20px;
    }
    .hero__title {
      font-size: 40px;
      font-weight: 700;
      line-height: 1.15;
      max-width: 600px;
      margin-bottom: 16px;
    }
    .hero__body {
      font-size: 15px;
      color: rgba(255,255,255,.75);
      max-width: 480px;
      margin-bottom: 28px;
    }
    .btn {
      display: inline-block;
      padding: 13px 28px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--white { background: #fff; color: var(--color-primary); }
    .btn--primary { background: var(--color-primary); color: #fff; }
    .btn--outline-white { border: 2px solid #fff; color: #fff; }

    /* ─── NEWSLETTER ─────────────────────────────── */
    .section-newsletter {
      background: var(--color-surface);
      padding: 48px 40px;
    }
    .section-newsletter__title { font-size: 22px; font-weight: 700; margin-bottom: 20px; }
    .form-stack { display: flex; flex-direction: column; gap: 12px; max-width: 400px; }
    .form-stack input {
      padding: 13px 16px;
      font-size: 13px;
      border: 1px solid #ddd;
      outline: none;
      background: #f8f8f8;
    }
    .form-stack input::placeholder { color: #aaa; }
    .form-stack button {
      padding: 13px 24px;
      background: var(--color-primary);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      border: none;
      cursor: pointer;
      align-self: flex-start;
    }

    /* ─── HERO IMAGE ─────────────────────────────── */
    .section-image { height: 280px; overflow: hidden; }

    /* ─── SECOND CTA ─────────────────────────────── */
    .section-cta {
      background: var(--color-primary);
      padding: 56px 40px;
      color: #fff;
    }
    .section-cta__title {
      font-size: 32px;
      font-weight: 700;
      line-height: 1.2;
      margin-bottom: 20px;
      max-width: 520px;
    }
    .section-cta__body { font-size: 14px; color: rgba(255,255,255,.75); margin-bottom: 28px; max-width: 480px; }

    /* ─── STATS ──────────────────────────────────── */
    .section-stats {
      background: var(--color-bg);
      padding: 48px 40px;
    }
    .stats { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; }
    .stat { padding: 28px; background: var(--color-surface); }
    .stat__number { font-size: 40px; font-weight: 900; color: var(--color-primary); margin-bottom: 6px; }
    .stat__label { font-size: 13px; color: var(--color-muted); }

    /* ─── SPEAKERS ───────────────────────────────── */
    .section-speakers {
      background: var(--color-surface);
      padding: 56px 40px;
    }
    .section-speakers__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-muted);
      margin-bottom: 28px;
    }
    .speakers-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 32px; }
    .speaker__avatar { width: 80px; height: 80px; border-radius: 50%; overflow: hidden; margin-bottom: 12px; }
    .speaker__name { font-size: 15px; font-weight: 700; margin-bottom: 4px; }
    .speaker__title { font-size: 12px; color: var(--color-muted); }

    /* ─── FINAL CTA ──────────────────────────────── */
    .section-final {
      padding: 64px 40px;
      background: var(--color-bg);
      text-align: center;
    }
    .section-final__title { font-size: 28px; font-weight: 700; margin-bottom: 12px; }
    .section-final__body { font-size: 14px; color: var(--color-muted); margin-bottom: 28px; }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 28px 40px;
      background: var(--color-dark);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer__logo img { height: 22px; width: auto; filter: brightness(0) invert(1); }
    .footer__dots { display: flex; gap: 6px; }
    .footer__dot { width: 7px; height: 7px; border-radius: 50%; background: #333; }
    .footer__dot.active { background: var(--color-primary); }
    .footer__legal { font-size: 11px; color: #555; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <a href="{{HEADER_CTA_URL}}" class="btn btn--white">{{HEADER_CTA_LABEL}}</a>
  </header>

  <!-- HERO -->
  <section class="hero">
    <span class="hero__tag">{{HERO_TAG_LABEL}}</span>
    <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
    <p class="hero__body">{{HERO_BODY}}</p>
    <a href="{{HERO_CTA_URL}}" class="btn btn--white">{{HERO_CTA_LABEL}}</a>
  </section>

  <!-- NEWSLETTER / SIGNUP -->
  <section class="section-newsletter">
    <h2 class="section-newsletter__title">{{SIGNUP_TITLE}}</h2>
    <form class="form-stack" action="{{FORM_ACTION_URL}}" method="POST">
      <input type="text" name="name" placeholder="{{FORM_NAME_PLACEHOLDER}}" />
      <input type="email" name="email" placeholder="{{FORM_EMAIL_PLACEHOLDER}}" required />
      <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
    </form>
  </section>

  <!-- HERO IMAGE -->
  <section class="section-image">
    <img src="{{EVENT_IMAGE_URL}}" alt="{{EVENT_IMAGE_ALT}}" />
  </section>

  <!-- SECOND CTA BLOCK -->
  <section class="section-cta">
    <h2 class="section-cta__title">{{CTA_2_TITLE}}</h2>
    <p class="section-cta__body">{{CTA_2_BODY}}</p>
    <a href="{{CTA_2_URL}}" class="btn btn--white">{{CTA_2_LABEL}}</a>
  </section>

  <!-- STATS -->
  <section class="section-stats">
    <div class="stats">
      <div class="stat">
        <div class="stat__number">{{STAT_1_VALUE}}</div>
        <p class="stat__label">{{STAT_1_LABEL}}</p>
      </div>
      <div class="stat">
        <div class="stat__number">{{STAT_2_VALUE}}</div>
        <p class="stat__label">{{STAT_2_LABEL}}</p>
      </div>
      <div class="stat">
        <div class="stat__number">{{STAT_3_VALUE}}</div>
        <p class="stat__label">{{STAT_3_LABEL}}</p>
      </div>
      <div class="stat">
        <div class="stat__number">{{STAT_4_VALUE}}</div>
        <p class="stat__label">{{STAT_4_LABEL}}</p>
      </div>
    </div>
  </section>

  <!-- SPEAKERS -->
  <section class="section-speakers">
    <p class="section-speakers__label">{{SPEAKERS_SECTION_LABEL}}</p>
    <div class="speakers-grid">
      <div class="speaker">
        <div class="speaker__avatar"><img src="{{SPEAKER_1_AVATAR_URL}}" alt="{{SPEAKER_1_NAME}}" /></div>
        <h3 class="speaker__name">{{SPEAKER_1_NAME}}</h3>
        <p class="speaker__title">{{SPEAKER_1_TITLE}}</p>
      </div>
      <div class="speaker">
        <div class="speaker__avatar"><img src="{{SPEAKER_2_AVATAR_URL}}" alt="{{SPEAKER_2_NAME}}" /></div>
        <h3 class="speaker__name">{{SPEAKER_2_NAME}}</h3>
        <p class="speaker__title">{{SPEAKER_2_TITLE}}</p>
      </div>
      <div class="speaker">
        <div class="speaker__avatar"><img src="{{SPEAKER_3_AVATAR_URL}}" alt="{{SPEAKER_3_NAME}}" /></div>
        <h3 class="speaker__name">{{SPEAKER_3_NAME}}</h3>
        <p class="speaker__title">{{SPEAKER_3_TITLE}}</p>
      </div>
    </div>
    <a href="{{ALL_SPEAKERS_URL}}" style="font-size:13px; color:var(--color-primary); text-decoration:underline;">{{ALL_SPEAKERS_LABEL}}</a>
  </section>

  <!-- FINAL CTA -->
  <section class="section-final">
    <h2 class="section-final__title">{{FINAL_CTA_TITLE}}</h2>
    <p class="section-final__body">{{FINAL_CTA_BODY}}</p>
    <a href="{{FINAL_CTA_URL}}" class="btn btn--primary">{{FINAL_CTA_LABEL}}</a>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__dots">
      <div class="footer__dot active"></div>
      <div class="footer__dot"></div>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

5. lp-agency-contact.html
Catégorie : Agency Contact — Usage : Agence, studio, service, formulaire de contact

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};           /* #e8ecff */
      --color-surface: {{COLOR_SURFACE}};  /* #ffffff */
      --color-dark: {{COLOR_DARK}};        /* #111111 */
      --color-accent: {{COLOR_ACCENT}};    /* #3355ff */
      --color-muted: {{COLOR_MUTED}};      /* #888888 */
      --font-main: {{FONT_MAIN}};          /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-dark);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 40px;
      background: var(--color-surface);
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid #dde0ff;
    }
    .header__logo img { height: 26px; width: auto; }
    .header__nav { display: flex; gap: 28px; }
    .header__nav a {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: var(--color-muted);
    }
    .header__nav a:hover { color: var(--color-dark); }
    .header__cta {
      background: var(--color-accent);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      padding: 10px 20px;
    }

    /* ─── HERO ───────────────────────────────────── */
    .hero { padding: 64px 40px 40px; max-width: 640px; }
    .hero__title {
      font-size: 42px;
      font-weight: 700;
      line-height: 1.1;
      letter-spacing: -1px;
      margin-bottom: 16px;
    }
    .hero__body {
      font-size: 15px;
      color: var(--color-muted);
      max-width: 480px;
      margin-bottom: 32px;
    }
    .btn {
      display: inline-block;
      padding: 12px 28px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--accent { background: var(--color-accent); color: #fff; }

    /* ─── PHOTO MOSAIC ───────────────────────────── */
    .section-mosaic { padding: 40px; }
    .mosaic-4 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 200px 200px;
      gap: 10px;
    }
    .mosaic-4 .m-large { grid-column: span 2; height: 260px; }
    .mosaic-4__item { overflow: hidden; }
    .mosaic-4__item img { transition: transform .4s; }
    .mosaic-4__item:hover img { transform: scale(1.04); }

    /* ─── ABOUT BLOCK ────────────────────────────── */
    .section-about {
      display: grid;
      grid-template-columns: 1fr 1fr;
      background: var(--color-surface);
    }
    .section-about__image { height: 380px; overflow: hidden; }
    .section-about__content {
      padding: 48px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .section-about__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-muted);
      margin-bottom: 16px;
    }
    .section-about__title { font-size: 26px; font-weight: 700; margin-bottom: 14px; line-height: 1.2; }
    .section-about__body { font-size: 13px; color: var(--color-muted); line-height: 1.8; }

    /* ─── CONTACT FORM ───────────────────────────── */
    .section-contact {
      padding: 64px 40px;
      background: var(--color-bg);
    }
    .contact-box {
      max-width: 560px;
      margin: 0 auto;
    }
    .contact-box__title { font-size: 28px; font-weight: 700; margin-bottom: 8px; }
    .contact-box__body { font-size: 13px; color: var(--color-muted); margin-bottom: 32px; }
    .form-contact { display: flex; flex-direction: column; gap: 14px; }
    .form-contact input,
    .form-contact textarea {
      padding: 13px 16px;
      font-size: 13px;
      border: 1px solid #c8ccff;
      background: var(--color-surface);
      outline: none;
      font-family: inherit;
    }
    .form-contact input::placeholder,
    .form-contact textarea::placeholder { color: #aaa; }
    .form-contact textarea { min-height: 120px; resize: vertical; }
    .form-contact button {
      padding: 14px 28px;
      background: var(--color-accent);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1px;
      border: none;
      cursor: pointer;
      align-self: flex-start;
    }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 28px 40px;
      background: var(--color-dark);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer__logo img { height: 22px; width: auto; filter: brightness(0) invert(1); }
    .footer__dots { display: flex; gap: 6px; }
    .footer__dot { width: 7px; height: 7px; border-radius: 50%; background: #333; }
    .footer__dot.active { background: var(--color-accent); }
    .footer__legal { font-size: 11px; color: #555; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <nav class="header__nav">
      <a href="{{NAV_LINK_1_URL}}">{{NAV_LINK_1_LABEL}}</a>
      <a href="{{NAV_LINK_2_URL}}">{{NAV_LINK_2_LABEL}}</a>
    </nav>
    <a href="{{HEADER_CTA_URL}}" class="btn btn--accent">{{HEADER_CTA_LABEL}}</a>
  </header>

  <!-- HERO -->
  <section class="hero">
    <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
    <p class="hero__body">{{HERO_BODY}}</p>
    <a href="{{HERO_CTA_URL}}" class="btn btn--accent">{{HERO_CTA_LABEL}}</a>
  </section>

  <!-- PHOTO MOSAIC -->
  <section class="section-mosaic">
    <div class="mosaic-4">
      <div class="mosaic-4__item m-large">
        <img src="{{MOSAIC_IMG_1_URL}}" alt="{{MOSAIC_IMG_1_ALT}}" />
      </div>
      <div class="mosaic-4__item">
        <img src="{{MOSAIC_IMG_2_URL}}" alt="{{MOSAIC_IMG_2_ALT}}" />
      </div>
      <div class="mosaic-4__item">
        <img src="{{MOSAIC_IMG_3_URL}}" alt="{{MOSAIC_IMG_3_ALT}}" />
      </div>
      <div class="mosaic-4__item">
        <img src="{{MOSAIC_IMG_4_URL}}" alt="{{MOSAIC_IMG_4_ALT}}" />
      </div>
      <div class="mosaic-4__item">
        <img src="{{MOSAIC_IMG_5_URL}}" alt="{{MOSAIC_IMG_5_ALT}}" />
      </div>
    </div>
  </section>

  <!-- ABOUT BLOCK -->
  <section class="section-about">
    <div class="section-about__image">
      <img src="{{ABOUT_IMAGE_URL}}" alt="{{ABOUT_IMAGE_ALT}}" />
    </div>
    <div class="section-about__content">
      <p class="section-about__label">{{ABOUT_LABEL}}</p>
      <h2 class="section-about__title">{{ABOUT_TITLE}}</h2>
      <p class="section-about__body">{{ABOUT_BODY}}</p>
    </div>
  </section>

  <!-- CONTACT FORM -->
  <section class="section-contact">
    <div class="contact-box">
      <h2 class="contact-box__title">{{CONTACT_TITLE}}</h2>
      <p class="contact-box__body">{{CONTACT_BODY}}</p>
      <form class="form-contact" action="{{FORM_ACTION_URL}}" method="POST">
        <input type="text" name="name" placeholder="{{FORM_NAME_PLACEHOLDER}}" />
        <input type="email" name="email" placeholder="{{FORM_EMAIL_PLACEHOLDER}}" required />
        <input type="text" name="subject" placeholder="{{FORM_SUBJECT_PLACEHOLDER}}" />
        <textarea name="message" placeholder="{{FORM_MESSAGE_PLACEHOLDER}}"></textarea>
        <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
      </form>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__dots">
      <div class="footer__dot active"></div>
      <div class="footer__dot"></div>
      <div class="footer__dot"></div>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

6. lp-product-launch.html
Catégorie : Product Launch — Usage : E-commerce, beauté, lifestyle, showcase produit + inscription

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};            /* #fff5f0 */
      --color-surface: {{COLOR_SURFACE}};   /* #ffffff */
      --color-dark: {{COLOR_DARK}};         /* #111111 */
      --color-accent: {{COLOR_ACCENT}};     /* #c83c28 */
      --color-muted: {{COLOR_MUTED}};       /* #888888 */
      --font-main: {{FONT_MAIN}};           /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-dark);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 40px;
      background: var(--color-surface);
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid #f0e0dc;
    }
    .header__logo img { height: 26px; width: auto; }
    .header__cta {
      background: var(--color-dark);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      padding: 10px 20px;
    }

    /* ─── HERO — 2 COL ───────────────────────────── */
    .hero {
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 400px;
    }
    .hero__content {
      padding: 56px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .hero__tag {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-accent);
      margin-bottom: 16px;
    }
    .hero__title {
      font-size: 36px;
      font-weight: 700;
      line-height: 1.15;
      letter-spacing: -0.5px;
      margin-bottom: 16px;
    }
    .hero__body {
      font-size: 14px;
      color: var(--color-muted);
      margin-bottom: 28px;
    }
    .hero__image { overflow: hidden; }
    .btn {
      display: inline-block;
      padding: 12px 28px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--dark { background: var(--color-dark); color: #fff; }
    .btn--accent { background: var(--color-accent); color: #fff; }
    .btn--outline { border: 1px solid var(--color-dark); color: var(--color-dark); }

    /* ─── INTRO PRODUCT ──────────────────────────── */
    .section-intro {
      padding: 56px 40px;
      background: var(--color-surface);
    }
    .section-intro__title {
      font-size: 22px;
      font-weight: 700;
      margin-bottom: 10px;
    }
    .section-intro__body { font-size: 14px; color: var(--color-muted); max-width: 560px; }

    /* ─── PRODUCTS LIST ──────────────────────────── */
    .section-products {
      padding: 0 40px 56px;
      background: var(--color-surface);
      display: flex;
      flex-direction: column;
      gap: 32px;
    }
    .product-row {
      display: grid;
      grid-template-columns: 240px 1fr;
      gap: 32px;
      align-items: center;
      padding-bottom: 32px;
      border-bottom: 1px solid #f0e0dc;
    }
    .product-row:last-child { border-bottom: none; }
    .product-row__image { height: 200px; overflow: hidden; }
    .product-row__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--color-muted);
      margin-bottom: 10px;
    }
    .product-row__title { font-size: 20px; font-weight: 700; margin-bottom: 8px; }
    .product-row__desc { font-size: 13px; color: var(--color-muted); margin-bottom: 16px; }
    .product-row__price { font-size: 18px; font-weight: 700; color: var(--color-accent); margin-bottom: 16px; }

    /* ─── GET ACCESS ─────────────────────────────── */
    .section-access {
      display: grid;
      grid-template-columns: 1fr 1fr;
      background: var(--color-dark);
      color: #fff;
    }
    .section-access__image { height: 360px; overflow: hidden; }
    .section-access__content {
      padding: 56px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .section-access__label {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: #888;
      margin-bottom: 14px;
    }
    .section-access__title { font-size: 26px; font-weight: 700; margin-bottom: 14px; }
    .section-access__body { font-size: 13px; color: #aaa; margin-bottom: 28px; }
    .form-access { display: flex; flex-direction: column; gap: 12px; }
    .form-access input {
      padding: 13px 16px;
      font-size: 13px;
      background: #1a1a1a;
      border: 1px solid #333;
      color: #fff;
      outline: none;
    }
    .form-access input::placeholder { color: #555; }
    .form-access button {
      padding: 13px 24px;
      background: var(--color-accent);
      color: #fff;
      font-size: 12px;
      font-weight: 700;
      border: none;
      cursor: pointer;
    }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 28px 40px;
      background: var(--color-surface);
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top: 1px solid #f0e0dc;
    }
    .footer__logo img { height: 22px; width: auto; }
    .footer__dots { display: flex; gap: 6px; }
    .footer__dot { width: 7px; height: 7px; border-radius: 50%; background: #ddd; }
    .footer__dot.active { background: var(--color-dark); }
    .footer__legal { font-size: 11px; color: #aaa; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <a href="{{HEADER_CTA_URL}}" class="btn btn--dark">{{HEADER_CTA_LABEL}}</a>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="hero__content">
      <p class="hero__tag">{{HERO_TAG_LABEL}}</p>
      <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
      <p class="hero__body">{{HERO_BODY}}</p>
      <a href="{{HERO_CTA_URL}}" class="btn btn--dark" style="align-self:flex-start;">{{HERO_CTA_LABEL}}</a>
    </div>
    <div class="hero__image">
      <img src="{{HERO_IMAGE_URL}}" alt="{{HERO_IMAGE_ALT}}" />
    </div>
  </section>

  <!-- INTRO -->
  <section class="section-intro">
    <h2 class="section-intro__title">{{INTRO_TITLE}}</h2>
    <p class="section-intro__body">{{INTRO_BODY}}</p>
  </section>

  <!-- PRODUCTS LIST -->
  <section class="section-products">
    <div class="product-row">
      <div class="product-row__image">
        <img src="{{PRODUCT_1_IMAGE_URL}}" alt="{{PRODUCT_1_NAME}}" />
      </div>
      <div>
        <p class="product-row__label">{{PRODUCT_1_CATEGORY}}</p>
        <h3 class="product-row__title">{{PRODUCT_1_NAME}}</h3>
        <p class="product-row__desc">{{PRODUCT_1_DESC}}</p>
        <p class="product-row__price">{{PRODUCT_1_PRICE}}</p>
        <a href="{{PRODUCT_1_URL}}" class="btn btn--outline">{{PRODUCT_1_CTA}}</a>
      </div>
    </div>
    <div class="product-row">
      <div class="product-row__image">
        <img src="{{PRODUCT_2_IMAGE_URL}}" alt="{{PRODUCT_2_NAME}}" />
      </div>
      <div>
        <p class="product-row__label">{{PRODUCT_2_CATEGORY}}</p>
        <h3 class="product-row__title">{{PRODUCT_2_NAME}}</h3>
        <p class="product-row__desc">{{PRODUCT_2_DESC}}</p>
        <p class="product-row__price">{{PRODUCT_2_PRICE}}</p>
        <a href="{{PRODUCT_2_URL}}" class="btn btn--outline">{{PRODUCT_2_CTA}}</a>
      </div>
    </div>
    <div class="product-row">
      <div class="product-row__image">
        <img src="{{PRODUCT_3_IMAGE_URL}}" alt="{{PRODUCT_3_NAME}}" />
      </div>
      <div>
        <p class="product-row__label">{{PRODUCT_3_CATEGORY}}</p>
        <h3 class="product-row__title">{{PRODUCT_3_NAME}}</h3>
        <p class="product-row__desc">{{PRODUCT_3_DESC}}</p>
        <p class="product-row__price">{{PRODUCT_3_PRICE}}</p>
        <a href="{{PRODUCT_3_URL}}" class="btn btn--outline">{{PRODUCT_3_CTA}}</a>
      </div>
    </div>
  </section>

  <!-- GET ACCESS -->
  <section class="section-access">
    <div class="section-access__image">
      <img src="{{ACCESS_IMAGE_URL}}" alt="{{ACCESS_IMAGE_ALT}}" />
    </div>
    <div class="section-access__content">
      <p class="section-access__label">{{ACCESS_LABEL}}</p>
      <h2 class="section-access__title">{{ACCESS_TITLE}}</h2>
      <p class="section-access__body">{{ACCESS_BODY}}</p>
      <form class="form-access" action="{{FORM_ACTION_URL}}" method="POST">
        <input type="email" name="email" placeholder="{{FORM_EMAIL_PLACEHOLDER}}" required />
        <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
      </form>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__dots">
      <div class="footer__dot active"></div>
      <div class="footer__dot"></div>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

7. lp-saas-bold.html
Catégorie : SaaS Bold — Usage : Startup, SaaS, tech, ton radical, typographie agressive

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{PAGE_TITLE}}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --color-bg: {{COLOR_BG}};            /* #0a0a0a */
      --color-surface: {{COLOR_SURFACE}};   /* #111111 */
      --color-accent: {{COLOR_ACCENT}};     /* #c8ff00 */
      --color-text: {{COLOR_TEXT}};         /* #eeeeee */
      --color-muted: {{COLOR_MUTED}};       /* #666666 */
      --font-main: {{FONT_MAIN}};           /* 'Helvetica Neue', Helvetica, Arial, sans-serif */
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-main);
      font-size: 14px;
      line-height: 1.7;
    }

    a { color: inherit; text-decoration: none; }
    img { display: block; width: 100%; height: 100%; object-fit: cover; }

    /* ─── HEADER ─────────────────────────────────── */
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 40px;
      background: var(--color-bg);
      border-bottom: 1px solid #1e1e1e;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .header__logo img { height: 24px; width: auto; }
    .header__cta {
      background: var(--color-accent);
      color: var(--color-bg);
      font-size: 11px;
      font-weight: 900;
      letter-spacing: 2px;
      text-transform: uppercase;
      padding: 10px 22px;
    }

    /* ─── HERO ───────────────────────────────────── */
    .hero {
      padding: 0;
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 460px;
      border-bottom: 1px solid #1e1e1e;
    }
    .hero__content {
      padding: 56px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      border-right: 1px solid #1e1e1e;
    }
    .hero__tag {
      display: inline-block;
      background: var(--color-accent);
      color: var(--color-bg);
      font-size: 10px;
      font-weight: 900;
      letter-spacing: 2px;
      text-transform: uppercase;
      padding: 4px 10px;
      margin-bottom: 20px;
      align-self: flex-start;
    }
    .hero__title {
      font-size: 46px;
      font-weight: 900;
      line-height: 1.05;
      letter-spacing: -2px;
      text-transform: uppercase;
      margin-bottom: 20px;
    }
    .hero__body {
      font-size: 14px;
      color: var(--color-muted);
      max-width: 380px;
      margin-bottom: 32px;
    }
    .hero__image { overflow: hidden; }
    .btn {
      display: inline-block;
      padding: 13px 28px;
      font-size: 11px;
      font-weight: 900;
      letter-spacing: 2px;
      text-transform: uppercase;
      cursor: pointer;
      transition: opacity .2s;
    }
    .btn:hover { opacity: .85; }
    .btn--accent { background: var(--color-accent); color: var(--color-bg); }
    .btn--outline { border: 1px solid #333; color: var(--color-text); }

    /* ─── SUBSCRIBE BAND ─────────────────────────── */
    .section-subscribe {
      background: var(--color-accent);
      padding: 28px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
    }
    .section-subscribe__title {
      font-size: 15px;
      font-weight: 900;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: var(--color-bg);
      flex: 1;
    }
    .form-inline { display: flex; gap: 0; }
    .form-inline input {
      padding: 12px 16px;
      font-size: 13px;
      background: rgba(0,0,0,.15);
      border: 1px solid rgba(0,0,0,.3);
      border-right: none;
      color: var(--color-bg);
      outline: none;
      width: 240px;
    }
    .form-inline input::placeholder { color: rgba(0,0,0,.5); }
    .form-inline button {
      padding: 12px 20px;
      background: var(--color-bg);
      color: var(--color-accent);
      font-size: 11px;
      font-weight: 900;
      letter-spacing: 1px;
      text-transform: uppercase;
      border: none;
      cursor: pointer;
    }

    /* ─── SERVICE BLOCKS ─────────────────────────── */
    .section-services { border-top: 1px solid #1e1e1e; }
    .service-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      border-bottom: 1px solid #1e1e1e;
      min-height: 300px;
    }
    .service-row__content {
      padding: 48px 40px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      border-right: 1px solid #1e1e1e;
    }
    .service-row__content.right {
      border-right: none;
      border-left: 1px solid #1e1e1e;
    }
    .service-row__image { overflow: hidden; height: 300px; }
    .service-row__tag {
      display: inline-block;
      background: var(--color-accent);
      color: var(--color-bg);
      font-size: 9px;
      font-weight: 900;
      letter-spacing: 2px;
      text-transform: uppercase;
      padding: 3px 8px;
      margin-bottom: 16px;
      align-self: flex-start;
    }
    .service-row__title {
      font-size: 28px;
      font-weight: 900;
      letter-spacing: -1px;
      text-transform: uppercase;
      line-height: 1.05;
      margin-bottom: 12px;
    }
    .service-row__body { font-size: 13px; color: var(--color-muted); margin-bottom: 20px; }

    /* ─── VALUE SECTION ──────────────────────────── */
    .section-value {
      background: var(--color-accent);
      padding: 56px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 32px;
    }
    .section-value__title {
      font-size: 36px;
      font-weight: 900;
      letter-spacing: -1px;
      text-transform: uppercase;
      color: var(--color-bg);
      max-width: 400px;
      line-height: 1.05;
    }
    .section-value__right { display: flex; flex-direction: column; gap: 12px; align-items: flex-start; }
    .section-value__body { font-size: 14px; color: rgba(0,0,0,.6); max-width: 360px; }

    /* ─── FOOTER ─────────────────────────────────── */
    .footer {
      padding: 28px 40px;
      background: var(--color-bg);
      border-top: 1px solid #1e1e1e;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer__logo img { height: 22px; width: auto; }
    .footer__dots { display: flex; gap: 6px; }
    .footer__dot { width: 7px; height: 7px; background: #222; }
    .footer__dot.active { background: var(--color-accent); }
    .footer__legal { font-size: 11px; color: #333; }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="header">
    <div class="header__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <a href="{{HEADER_CTA_URL}}" class="btn btn--accent">{{HEADER_CTA_LABEL}}</a>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="hero__content">
      <span class="hero__tag">{{HERO_TAG_LABEL}}</span>
      <h1 class="hero__title">{{HERO_HEADLINE}}</h1>
      <p class="hero__body">{{HERO_BODY}}</p>
      <a href="{{HERO_CTA_URL}}" class="btn btn--accent" style="align-self:flex-start;">{{HERO_CTA_LABEL}}</a>
    </div>
    <div class="hero__image">
      <img src="{{HERO_IMAGE_URL}}" alt="{{HERO_IMAGE_ALT}}" />
    </div>
  </section>

  <!-- SUBSCRIBE BAND -->
  <section class="section-subscribe">
    <p class="section-subscribe__title">{{SUBSCRIBE_HEADLINE}}</p>
    <form class="form-inline" action="{{FORM_ACTION_URL}}" method="POST">
      <input type="email" name="email" placeholder="{{FORM_PLACEHOLDER}}" required />
      <button type="submit">{{FORM_SUBMIT_LABEL}}</button>
    </form>
  </section>

  <!-- SERVICES -->
  <section class="section-services">

    <!-- Service 1 — content left, image right -->
    <div class="service-row">
      <div class="service-row__content">
        <div>
          <span class="service-row__tag">{{SERVICE_1_TAG}}</span>
          <h2 class="service-row__title">{{SERVICE_1_TITLE}}</h2>
          <p class="service-row__body">{{SERVICE_1_BODY}}</p>
        </div>
        <a href="{{SERVICE_1_CTA_URL}}" class="btn btn--accent" style="align-self:flex-start;">{{SERVICE_1_CTA_LABEL}}</a>
      </div>
      <div class="service-row__image">
        <img src="{{SERVICE_1_IMAGE_URL}}" alt="{{SERVICE_1_TITLE}}" />
      </div>
    </div>

    <!-- Service 2 — image left, content right -->
    <div class="service-row">
      <div class="service-row__image">
        <img src="{{SERVICE_2_IMAGE_URL}}" alt="{{SERVICE_2_TITLE}}" />
      </div>
      <div class="service-row__content right">
        <div>
          <span class="service-row__tag">{{SERVICE_2_TAG}}</span>
          <h2 class="service-row__title">{{SERVICE_2_TITLE}}</h2>
          <p class="service-row__body">{{SERVICE_2_BODY}}</p>
        </div>
        <a href="{{SERVICE_2_CTA_URL}}" class="btn btn--accent" style="align-self:flex-start;">{{SERVICE_2_CTA_LABEL}}</a>
      </div>
    </div>

    <!-- Service 3 — content left, image right -->
    <div class="service-row">
      <div class="service-row__content">
        <div>
          <span class="service-row__tag">{{SERVICE_3_TAG}}</span>
          <h2 class="service-row__title">{{SERVICE_3_TITLE}}</h2>
          <p class="service-row__body">{{SERVICE_3_BODY}}</p>
        </div>
        <a href="{{SERVICE_3_CTA_URL}}" class="btn btn--accent" style="align-self:flex-start;">{{SERVICE_3_CTA_LABEL}}</a>
      </div>
      <div class="service-row__image">
        <img src="{{SERVICE_3_IMAGE_URL}}" alt="{{SERVICE_3_TITLE}}" />
      </div>
    </div>

  </section>

  <!-- VALUE PROPOSITION -->
  <section class="section-value">
    <h2 class="section-value__title">{{VALUE_HEADLINE}}</h2>
    <div class="section-value__right">
      <p class="section-value__body">{{VALUE_BODY}}</p>
      <a href="{{VALUE_CTA_URL}}" class="btn" style="background:var(--color-bg); color:var(--color-accent); align-self:flex-start;">
        {{VALUE_CTA_LABEL}}
      </a>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer__logo">
      <a href="{{LOGO_LINK_URL}}">
        <img src="{{LOGO_URL}}" alt="{{BRAND_NAME}}" />
      </a>
    </div>
    <div class="footer__dots">
      <div class="footer__dot active"></div>
      <div class="footer__dot"></div>
    </div>
    <p class="footer__legal">© {{CURRENT_YEAR}} {{BRAND_NAME}}</p>
  </footer>

</body>
</html>

Récapitulatif des 7 landing pages
#	Fichier	Catégorie	Couleur dominante
1	lp-editorial-dark.html	Editorial Magazine	Noir #0a0a0a
2	lp-content-newsletter.html	Content Newsletter	Vert clair #f0fce8
3	lp-creative-portfolio.html	Creative Portfolio	Crème #fff8f0
4	lp-event-registration.html	Event Registration	Violet #7c3aed
5	lp-agency-contact.html	Agency Contact	Lavande #e8ecff
6	lp-product-launch.html	Product Launch	Rose poudré #fff5f0
7	lp-saas-bold.html	SaaS Bold	Noir + Neon #c8ff00
Convention des placeholders : toutes les variables {{COULEUR}}, {{URL}}, {{TEXTE}} sont substituables directement dans l'HTML — aucune dépendance JS ni framework.


main

claude/review-github-repo-jU36w

+51
-3

Voir la PR



Ora

